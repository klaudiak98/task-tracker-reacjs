{
  "name": "react-task-tracker",
  "version": "0.2.0",
  "private": true,
  "homepage": "https://github.com/klaudiak98/tast-tracker-reacjs",
  "dependencies": {
    "@vitejs/plugin-react": "^4.1.0",
    "json-server": "^0.16.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^4.2.0",
    "react-router-dom": "^6.16.0",
    "vite": "^4.4.11"
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "server": "json-server --watch db.json --port 5000",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
